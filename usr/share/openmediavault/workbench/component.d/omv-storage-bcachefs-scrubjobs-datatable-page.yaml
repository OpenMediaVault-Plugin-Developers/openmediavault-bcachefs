version: "1.0"
type: component
data:
  name: omv-storage-bcachefs-scrubjobs-datatable-page
  type: datatablePage
  config:
    autoReload: false
    hasSearchField: true
    stateId: d4e5f6a7-b8c9-0123-defa-123456789012
    store:
      proxy:
        service: Bcachefs
        get:
          method: getScrubJobList
    columns:
      - name: _("Enabled")
        prop: enable
        flexGrow: 1
        sortable: true
        cellTemplateName: checkIcon
      - name: _("Filesystem")
        prop: filesystem
        flexGrow: 3
        sortable: true
      - name: _("Scheduling")
        prop: ""
        flexGrow: 2
        cellTemplateName: template
        cellTemplateConfig: |
          {% if execution == "exactly" %}
          {% set _minute = minute %}
          {% set _hour = hour %}
          {% set _dayofmonth = dayofmonth %}
          {% if everynminute %}{% set _minute %}*/{{ minute }}{% endset %}{% endif %}
          {% if everynhour %}{% set _hour %}*/{{ hour }}{% endset %}{% endif %}
          {% if everyndayofmonth %}{% set _dayofmonth %}*/{{ dayofmonth }}{% endset %}{% endif %}
          {{ [_minute, _hour, _dayofmonth, month, dayofweek] | join(" ") | cron2human }}
          {% else %}
          {{ execution | capitalize | translate }}
          {% endif %}
      - name: _("Comment")
        prop: comment
        flexGrow: 2
        sortable: true
    actions:
      - template: create
        execute:
          type: url
          url: "/storage/bcachefs/scrubjobs/create"
      - template: edit
        execute:
          type: url
          url: "/storage/bcachefs/scrubjobs/edit/{{ _selected[0].uuid }}"
      - template: delete
        execute:
          type: request
          request:
            service: Bcachefs
            method: deleteScrubJob
            params:
              uuid: "{{ _selected[0].uuid }}"
      - type: iconButton
        tooltip: _("Run now")
        icon: mdi:play-box-outline
        enabledConstraints:
          minSelected: 1
          maxSelected: 1
        execute:
          type: request
          request:
            service: Bcachefs
            method: runScrubJob
            params:
              uuid: "{{ _selected[0].uuid }}"
            task: false
            progressMessage: _("Starting scrub ...")
            successNotification: _("Scrub started.")
